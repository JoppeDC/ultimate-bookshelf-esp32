#pragma once

#define LED_PIN     5        // Pin the strip is hooked up to
#define NUM_LEDS    60       // Total LEDs in your strip
#define LED_TYPE    WS2812B  // Type of LED strip. Check FASTLED config to see whats supported
#define COLOR_ORDER GRB      // Adjust if the colors on the strip dont match what you expect